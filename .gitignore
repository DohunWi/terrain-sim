# --- C++ / CMake ---
build/
build-tsan/
*.pgm
cmake-build-*/
CMakeCache.txt
CMakeFiles/
CMakeScripts/
Testing/
Makefile
cmake_install.cmake
compile_commands.json
.cache/
*.o
*.obj
*.exe
*.out
*.dSYM/

# --- Unity ---
unity-client/Library/
unity-client/Temp/
unity-client/Obj/
unity-client/Build/
unity-client/Builds/
unity-client/Logs/
unity-client/MemoryCaptures/
unity-client/UserSettings/
unity-client/*.csproj
unity-client/*.sln
unity-client/*.slnx
unity-client/.vs/

# --- Editor / OS ---
.DS_Store
.vscode/
.idea/

# --- Benchmarks output (raw runs, keep summarized reports only) ---
benchmarks/raw/

# --- Scratch C++ practice (not part of the project) ---
practice/

# --- Python (tools/, training/) ---
__pycache__/
*.pyc

# --- RL training artifacts (training/) ---
# docs/evaluation-protocol.md §11: models/raw logs stay local per experiment ID
# under training/artifacts/<experiment-id>/, only structured eval results
# (benchmarks/evaluations/) and representative trajectories go in git.
training/*.zip
training/runs/
training/logs/
training/tb_logs/
training/artifacts/

# --- RL replay data (regenerated by training/eval.py --dump-trajectory) ---
unity-client/Assets/StreamingAssets/*.json
unity-client/Assets/StreamingAssets/*.json.meta

# claude
CLAUDE.md
.claude/

# codex
AGENTS.md
# --- Internal-only docs (planning notes, worklog — not for public repo) ---
docs/private/
